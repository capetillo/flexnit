import React from 'react';


const SaveButton = (props) =>  (
 
    <SaveButton
      message={
        <span>Saved to your watchlist!</span>}
      action={
        <Button onClick={props.}>
          
        </Button>
      }

    />

);

    

export default SaveButton